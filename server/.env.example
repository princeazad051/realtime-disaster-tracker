# Express Server Configuration
PORT=5001
NODE_ENV=production

# Allowed CORS Origins (optional, comma-separated or single domain)
# Example: https://disaster-tracker.vercel.app,http://localhost:3000
ALLOWED_ORIGINS=*
CLIENT_URL=
